{
  "telegram": {
    "bot_token": "${TELEGRAM_BOT_TOKEN}",
    "admin_users": "${TELEGRAM_ADMIN_USERS}"
  },
  "_note": "Secrets are loaded from ENV or encrypted config (config/secrets/bot_secrets.enc)."
}
